package de.fhdortmund.ese.lib.simulation.entity.device;

public enum DeviceState {
    ON_ACTIVE,   // Device is on and actively consuming energy
    ON_IDLE,     // Device is on but not consuming energy
    OFF          // Device is off 
}
